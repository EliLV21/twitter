var publicaciones = [];
var contador = 1;

function contador(){
  var publicacion = document.getElementById("contador").value;
  var letras = publicaciones.length;
  var contador = 
  //document.createTextNode()
    //for(var i=0;i=>letras;i++){
      //  document.getElementById("numLetras").innerHTML = letras;
    //}
    //document.forms[0].numLetras.value=document.forms[0].textoTweets.value.length
  };

function tweets(seccion,texto,remitente,muestra,muestra1){
  var seccion = document.createElement("section");
  var texto = document.getElementById("textoTweets").value;
  var remitente = document.getElementById("contacto").value;
  var muestra = document.createTextNode(texto);
  var muestra1 = document.createTextNode(texto);

  seccion.appendChild(muestra);
  seccion.appendChild(muestra1);
};
//function(){};
